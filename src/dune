
(library
  (name Redu)
  (libraries str flex belt fluid RexJson fluid.Files fluid.MacOS odate)
  (c_names Redu)
  (flags :standard -open RexJson -w -A)
  (c_flags -x objective-c)
  (preprocess (pps belt.ppx fluid.ppx let-anything-esy)))

